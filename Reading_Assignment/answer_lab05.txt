- The Aims class must be updated to handle any exceptions generated when the play() methods are called. What happens when you don’t update for them to catch?
The error "The method play() from the type Playable refers to the missing type PlayerException" may appear when we attempt to use Aims if we don't update them to catch.